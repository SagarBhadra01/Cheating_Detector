"""
Dashboard package — FastAPI-based real-time proctoring dashboard.

Usage (local dev):
    uvicorn src.dashboard.app:app --reload

Usage (Render / production):
    uvicorn src.dashboard.app:app --host 0.0.0.0 --port $PORT
"""
